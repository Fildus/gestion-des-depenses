<form method="post" action="{{ path('source_account_delete', {'id': source_account.id}) }}" onsubmit="return confirm('êtes vous sûr de vouloir supprimer ce compte?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source_account.id) }}">
    <button class="btn">Supprimer</button>
</form>
