{% extends 'base.html.twig' %}

{% block title %}Compte source{% endblock %}

{% block body %}
    <h1>Compte source</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ source_account.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('source_account_index') }}">Retour à la liste</a>

    <a href="{{ path('source_account_edit', {'id': source_account.id}) }}">Editer</a>

    {{ include('source_account/_delete_form.html.twig') }}
{% endblock %}
