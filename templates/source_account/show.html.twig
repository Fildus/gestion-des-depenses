{% extends 'base.html.twig' %}

{% block title %}SourceAccount{% endblock %}

{% block body %}
    <h1>SourceAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ source_account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ source_account.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('source_account_index') }}">back to list</a>

    <a href="{{ path('source_account_edit', {'id': source_account.id}) }}">edit</a>

    {{ include('source_account/_delete_form.html.twig') }}
{% endblock %}
